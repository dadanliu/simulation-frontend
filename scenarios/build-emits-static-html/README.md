# build-emits-static-html

这个场景在线性主链中的位置：**Stage 2 构建处理阶段**。

## 它模拟的真实节点

内容在构建期提前确定，并被产出为适合静态分发的结果。

## 输入

- 构建时已知的数据
- 页面模板

## 输出

- 构建期提前生成的页面结果
- 更适合放到 CDN 的静态资源语义

## 观察点

- 页面内容不依赖请求期重新生成。
- 更适合配合 `pnpm build` 理解静态产物语义。
