# browser-receives-server-component-result

这个场景在线性主链中的位置：**Stage 4 首屏生成前半段**。

## 它模拟的真实节点

服务端组件在服务端读取数据，并把结果直接参与首屏输出。

## 输入

- 请求进入服务端运行时
- 服务端可访问的数据源
- 页面组件树

## 输出

- 已经带数据的首屏 HTML 结果
- 浏览器可见的内容

## 观察点

- 页面里没有客户端交互逻辑。
- 数据是在服务端准备好后进入首屏结果。
- 浏览器消费的是结果，而不是服务端取数函数本身。
