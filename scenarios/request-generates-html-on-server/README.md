# request-generates-html-on-server

这个场景在线性主链中的位置：**Stage 4 首屏生成节点**。

## 它模拟的真实节点

请求到来时重新计算页面首屏结果。

## 输入

- 当前请求时刻
- 服务端组件逻辑

## 输出

- 与本次请求绑定的首屏 HTML

## 观察点

- 多次刷新后 `requestedAt` 会变化。
- 说明页面结果依赖请求当下，而不是构建期固定结果。
